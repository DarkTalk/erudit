# Upstash Redis — создаётся автоматически при подключении Redis в Vercel Dashboard
# Storage → Redis → Create → Connect to Project
# Для локальной разработки скопируйте значения в .env.local
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
